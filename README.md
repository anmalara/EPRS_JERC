# EPR budget summary for JERC

**Instructions**
1. Setup:
    - Install PrettyTable -  `python -m pip install -U prettytable` according to https://pypi.org/project/prettytable/ 

2. Run:
    `./EPRsBudget.py`
    Options:
        - '-e/--expand' print all tables. Disabled as default. 
